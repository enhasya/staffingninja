import React from "react";

const NavbarUI = () => {
  return (
    <nav className="flex flex-col lg:flex-row gap-4 items-center justify-center w-full h-auto">
      NavbarUI
    </nav>
  );
};

export default NavbarUI;
